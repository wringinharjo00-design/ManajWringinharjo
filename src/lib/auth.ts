'use client';

// This file is now managed by Firebase client-side authentication.
// The `useUser` hook should be used in client components to get the current user state.
// The user's profile data, including their role, should be fetched from the `/users/{userId}` document in Firestore.
export {};
